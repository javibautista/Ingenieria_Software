languaje: python
python:
 - "3.5"
# command to install dependencies
install: "pip install --user coverage"
# command to run tests
script:
- coverage run test.py
- coverage run test_sut.py
- coverage report
