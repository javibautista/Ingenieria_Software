languaje: python
python:
 - "3.5"
# command to install dependencies
install: #"pip install --user mock coverage"
- pip install --user mock
- pip install --user coverage
# command to run tests
script:
- coverage run testing/test.py
- coverage run testing/test_sut.py
- coverage report
